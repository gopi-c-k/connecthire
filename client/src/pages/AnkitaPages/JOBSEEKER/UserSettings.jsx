export default function UserSettings() {
  return (
    <div className="space-y-3">
      <h1 className="text-2xl font-semibold">Settings</h1>
      <p className="text-muted">Notification & preferences (UI-only for now).</p>
    </div>
  );
}
